apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: key-create-cluster
# Validates basic Key CRUD lifecycle for cluster-scoped resources
# (fusionauth.crossplane.io). Creates a Key, asserts Ready, then cleans up.
spec:
  timeouts:
    delete: 5m
    cleanup: 5m
  steps:
    - name: create-key
      try:
        - apply:
            resource:
              apiVersion: fusionauth.crossplane.io/v1alpha1
              kind: Key
              metadata:
                name: e2e-test-key
                labels:
                  e2e-test: "true"
              spec:
                forProvider:
                  algorithm: RS256
                  name: e2e-test-key
                  length: 2048
                providerConfigRef:
                  name: default
        - assert:
            timeout: 5m
            resource:
              apiVersion: fusionauth.crossplane.io/v1alpha1
              kind: Key
              metadata:
                name: e2e-test-key
              status:
                (conditions[?type == 'Ready']):
                  - status: "True"
      finally:
        - delete:
            ref:
              apiVersion: fusionauth.crossplane.io/v1alpha1
              kind: Key
              name: e2e-test-key
