---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: connectors.ldap.fusionauth.crossplane.io
spec:
  group: ldap.fusionauth.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - fusionauth
    kind: Connector
    listKind: ConnectorList
    plural: connectors
    singular: connector
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connector is the Schema for the Connectors API. LDAP Connector
          Resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectorSpec defines the desired state of Connector
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  authenticationUrl:
                    description: |-
                      The fully qualified LDAP URL to authenticate.
                    type: string
                  baseStructure:
                    description: |-
                      The top of the LDAP directory hierarchy. Typically this contains the dc (domain component) element.
                    type: string
                  connectTimeout:
                    description: |-
                      The connect timeout for the HTTP connection, in milliseconds. Value must be greater than 0.
                    format: int64
                    type: integer
                  data:
                    description: |-
                      A JSON string that can hold any information about the Connector that should be persisted.
                      An object that can hold any information about the Connector that should be persisted.. Must be a JSON encoded string.
                    type: string
                  debug:
                    description: |-
                      Determines if debug should be enabled to create an event log to assist in debugging integration errors.
                      Enable debug logging.
                    type: boolean
                  identifyingAttribute:
                    description: |-
                      The entry attribute name which is the first component of the distinguished name of entries in the directory.
                    type: string
                  lambdaConfiguration:
                    items:
                      properties:
                        reconcileId:
                          description: |-
                            The Id of a Lambda. The lambda is executed after the user authenticates with the connector. This lambda can create a user, registrations, and group memberships in FusionAuth based on attributes returned from the connector.
                            The Id of an existing Lambda. The lambda is executed after the user authenticates with the connector. This lambda can create a user, registrations, and group memberships in FusionAuth based on attributes returned from the connector.
                          type: string
                        reconcileIdRef:
                          description: Reference to a Lambda in fusionauth to populate
                            reconcileId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        reconcileIdSelector:
                          description: Selector for a Lambda in fusionauth to populate
                            reconcileId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  loginIdAttribute:
                    description: |-
                      The entity attribute name which stores the identifier that is used for logging the user in.
                    type: string
                  name:
                    description: |-
                      The unique LDAP Connector name.
                    type: string
                  readTimeout:
                    description: |-
                      The read timeout for the HTTP connection, in milliseconds. Value must be greater than 0.
                    format: int64
                    type: integer
                  requestedAttributes:
                    description: |-
                      The list of attributes to request from the LDAP server. This is a list of strings.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  securityMethod:
                    description: |-
                      The LDAP security method. Possible values are: None (Requests will be made without encryption), LDAPS (A secure connection will be made to a secure port over using the LDAPS protocol) or StartTLS (An un-secured connection will initially be established, followed by secure connection established using the StartTLS extension).
                    type: string
                  systemAccountDn:
                    description: |-
                      The distinguished name of an entry that has read access to the directory.
                      The distinguished name of the system account used to authenticate to the LDAP server. This account must have permission to search the directory.
                    type: string
                  systemAccountPasswordSecretRef:
                    description: |-
                      The password of an entry that has read access to the directory.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authenticationUrl:
                    description: |-
                      The fully qualified LDAP URL to authenticate.
                    type: string
                  baseStructure:
                    description: |-
                      The top of the LDAP directory hierarchy. Typically this contains the dc (domain component) element.
                    type: string
                  connectTimeout:
                    description: |-
                      The connect timeout for the HTTP connection, in milliseconds. Value must be greater than 0.
                    format: int64
                    type: integer
                  data:
                    description: |-
                      A JSON string that can hold any information about the Connector that should be persisted.
                      An object that can hold any information about the Connector that should be persisted.. Must be a JSON encoded string.
                    type: string
                  debug:
                    description: |-
                      Determines if debug should be enabled to create an event log to assist in debugging integration errors.
                      Enable debug logging.
                    type: boolean
                  identifyingAttribute:
                    description: |-
                      The entry attribute name which is the first component of the distinguished name of entries in the directory.
                    type: string
                  lambdaConfiguration:
                    items:
                      properties:
                        reconcileId:
                          description: |-
                            The Id of a Lambda. The lambda is executed after the user authenticates with the connector. This lambda can create a user, registrations, and group memberships in FusionAuth based on attributes returned from the connector.
                            The Id of an existing Lambda. The lambda is executed after the user authenticates with the connector. This lambda can create a user, registrations, and group memberships in FusionAuth based on attributes returned from the connector.
                          type: string
                        reconcileIdRef:
                          description: Reference to a Lambda in fusionauth to populate
                            reconcileId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        reconcileIdSelector:
                          description: Selector for a Lambda in fusionauth to populate
                            reconcileId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                  loginIdAttribute:
                    description: |-
                      The entity attribute name which stores the identifier that is used for logging the user in.
                    type: string
                  name:
                    description: |-
                      The unique LDAP Connector name.
                    type: string
                  readTimeout:
                    description: |-
                      The read timeout for the HTTP connection, in milliseconds. Value must be greater than 0.
                    format: int64
                    type: integer
                  requestedAttributes:
                    description: |-
                      The list of attributes to request from the LDAP server. This is a list of strings.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  securityMethod:
                    description: |-
                      The LDAP security method. Possible values are: None (Requests will be made without encryption), LDAPS (A secure connection will be made to a secure port over using the LDAPS protocol) or StartTLS (An un-secured connection will initially be established, followed by secure connection established using the StartTLS extension).
                    type: string
                  systemAccountDn:
                    description: |-
                      The distinguished name of an entry that has read access to the directory.
                      The distinguished name of the system account used to authenticate to the LDAP server. This account must have permission to search the directory.
                    type: string
                  systemAccountPasswordSecretRef:
                    description: |-
                      The password of an entry that has read access to the directory.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                required:
                - systemAccountPasswordSecretRef
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.authenticationUrl is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.authenticationUrl)
                || (has(self.initProvider) && has(self.initProvider.authenticationUrl))'
            - message: spec.forProvider.baseStructure is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.baseStructure)
                || (has(self.initProvider) && has(self.initProvider.baseStructure))'
            - message: spec.forProvider.connectTimeout is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.connectTimeout)
                || (has(self.initProvider) && has(self.initProvider.connectTimeout))'
            - message: spec.forProvider.identifyingAttribute is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.identifyingAttribute)
                || (has(self.initProvider) && has(self.initProvider.identifyingAttribute))'
            - message: spec.forProvider.lambdaConfiguration is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.lambdaConfiguration)
                || (has(self.initProvider) && has(self.initProvider.lambdaConfiguration))'
            - message: spec.forProvider.loginIdAttribute is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.loginIdAttribute)
                || (has(self.initProvider) && has(self.initProvider.loginIdAttribute))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.readTimeout is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.readTimeout)
                || (has(self.initProvider) && has(self.initProvider.readTimeout))'
            - message: spec.forProvider.requestedAttributes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.requestedAttributes)
                || (has(self.initProvider) && has(self.initProvider.requestedAttributes))'
            - message: spec.forProvider.securityMethod is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.securityMethod)
                || (has(self.initProvider) && has(self.initProvider.securityMethod))'
            - message: spec.forProvider.systemAccountDn is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.systemAccountDn)
                || (has(self.initProvider) && has(self.initProvider.systemAccountDn))'
            - message: spec.forProvider.systemAccountPasswordSecretRef is a required
                parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.systemAccountPasswordSecretRef)'
          status:
            description: ConnectorStatus defines the observed state of Connector.
            properties:
              atProvider:
                properties:
                  authenticationUrl:
                    description: |-
                      The fully qualified LDAP URL to authenticate.
                    type: string
                  baseStructure:
                    description: |-
                      The top of the LDAP directory hierarchy. Typically this contains the dc (domain component) element.
                    type: string
                  connectTimeout:
                    description: |-
                      The connect timeout for the HTTP connection, in milliseconds. Value must be greater than 0.
                    format: int64
                    type: integer
                  data:
                    description: |-
                      A JSON string that can hold any information about the Connector that should be persisted.
                      An object that can hold any information about the Connector that should be persisted.. Must be a JSON encoded string.
                    type: string
                  debug:
                    description: |-
                      Determines if debug should be enabled to create an event log to assist in debugging integration errors.
                      Enable debug logging.
                    type: boolean
                  id:
                    type: string
                  identifyingAttribute:
                    description: |-
                      The entry attribute name which is the first component of the distinguished name of entries in the directory.
                    type: string
                  lambdaConfiguration:
                    items:
                      properties:
                        reconcileId:
                          description: |-
                            The Id of a Lambda. The lambda is executed after the user authenticates with the connector. This lambda can create a user, registrations, and group memberships in FusionAuth based on attributes returned from the connector.
                            The Id of an existing Lambda. The lambda is executed after the user authenticates with the connector. This lambda can create a user, registrations, and group memberships in FusionAuth based on attributes returned from the connector.
                          type: string
                      type: object
                    type: array
                  loginIdAttribute:
                    description: |-
                      The entity attribute name which stores the identifier that is used for logging the user in.
                    type: string
                  name:
                    description: |-
                      The unique LDAP Connector name.
                    type: string
                  readTimeout:
                    description: |-
                      The read timeout for the HTTP connection, in milliseconds. Value must be greater than 0.
                    format: int64
                    type: integer
                  requestedAttributes:
                    description: |-
                      The list of attributes to request from the LDAP server. This is a list of strings.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  securityMethod:
                    description: |-
                      The LDAP security method. Possible values are: None (Requests will be made without encryption), LDAPS (A secure connection will be made to a secure port over using the LDAPS protocol) or StartTLS (An un-secured connection will initially be established, followed by secure connection established using the StartTLS extension).
                    type: string
                  systemAccountDn:
                    description: |-
                      The distinguished name of an entry that has read access to the directory.
                      The distinguished name of the system account used to authenticate to the LDAP server. This account must have permission to search the directory.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
