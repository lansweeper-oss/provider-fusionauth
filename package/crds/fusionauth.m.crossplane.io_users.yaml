---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: users.fusionauth.m.crossplane.io
spec:
  group: fusionauth.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - fusionauth
    kind: User
    listKind: UserList
    plural: users
    singular: user
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: User is the Schema for the Users API. User Resource
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserSpec defines the desired state of User
            properties:
              forProvider:
                properties:
                  applicationId:
                    description: |-
                      An optional Application Id. When this value is provided, it will be used to resolve an application specific email template if you have configured transactional emails such as setup password, email verification and others.
                    type: string
                  birthDate:
                    description: |-
                      An ISO-8601 formatted date of the User’s birthdate such as YYYY-MM-DD.
                    type: string
                  data:
                    description: |-
                      A JSON string that can hold any information about a User that should be persisted. Must be a JSON serialised string.
                      An object that can hold any information about a User that should be persisted. Must be a JSON encoded string.
                    type: string
                  disableDomainBlock:
                    description: |-
                      A tenant has the option to configure one or more email domains to be blocked in order to restrict email domains during user create or update. Setting this property equal to true will override the tenant configuration. See registration_configuration.blocked_domains in the Tenant resource.
                      A tenant has the option to configure one or more email domains to be blocked in order to restrict email domains during user create or update.
                    type: boolean
                  email:
                    description: |-
                      The User’s email address. An email address is a unique in FusionAuth and stored in lower case.
                    type: string
                  encryptionScheme:
                    description: |-
                      The method for encrypting the User’s password.
                    type: string
                  expiry:
                    description: |-
                      The expiration instant of the User’s account. An expired user is not permitted to login.
                    format: int64
                    type: integer
                  factor:
                    description: The factor used by the password encryption scheme.
                      If not provided, the PasswordEncryptor provides a default value.
                      Generally this will be used as an iteration count to generate
                      the hash. The actual use of this value is up to the PasswordEncryptor
                      implementation.
                    format: int64
                    type: integer
                  firstName:
                    description: |-
                      The first name of the User.
                    type: string
                  fullName:
                    description: |-
                      The User’s full name as a separate field that is not calculated from firstName and lastName.
                    type: string
                  imageUrl:
                    description: |-
                      The URL that points to an image file that is the User’s profile image.
                    type: string
                  lastName:
                    description: |-
                      The User’s last name.
                    type: string
                  middleName:
                    description: |-
                      The User’s middle name.
                    type: string
                  mobilePhone:
                    description: |-
                      The User’s mobile phone number. This is useful is you will be sending push notifications or SMS messages to the User.
                    type: string
                  parentEmail:
                    description: |-
                      The email address of the user’s parent or guardian. This field is used to allow a child user to identify their parent so FusionAuth can make a request to the parent to confirm the parent relationship.
                    type: string
                  passwordChangeRequired:
                    description: |-
                      Indicates that the User’s password needs to be changed during their next login attempt.
                    type: boolean
                  passwordSecretRef:
                    description: |-
                      The User’s plaintext password. This password will be hashed and the provided value will never be stored and cannot be retrieved.
                      The User's plaintext password. This password will be hashed and the provided value will never be stored and cannot be retrieved.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  phoneNumber:
                    description: |-
                      The phone number of the User. The phone number is stored and returned in E.164 canonical format, however a phone number is considered unique regardless of the format. 303-555-1212 is considered equal to +13035551212 so either version of this phone number can be used whenever providing it as input to an API. If phone_number is not provided, then email or username will be required.
                    type: string
                  preferredLanguages:
                    description: |-
                      An array of locale strings that give, in order, the User’s preferred languages. These are important for email templates and other localizable text.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  sendSetPasswordEmail:
                    description: |-
                      Indicates to FusionAuth to send the User an email asking them to set their password. The Email Template that is used is configured in the System Configuration setting for Set Password Email Template. Use send_set_password_identity_type instead.
                      Indicates to FusionAuth to send the User an email asking them to set their password. The Email Template that is used is configured in the System Configuration setting for Set Password Email Template.
                    type: boolean
                  sendSetPasswordIdentityType:
                    description: |-
                      If set, FusionAuth will send the User a message asking them to set their password. When you set this value to anything but `doNotSend`, any provided password field is ignored. The possible values are `doNotSend`, `email`, and `phone`.
                    type: string
                  skipVerification:
                    description: |-
                      Indicates to FusionAuth that it should skip email verification even if it is enabled. This is useful for creating admin or internal User accounts.
                    type: boolean
                  tenantId:
                    description: |-
                      The unique Id of the tenant used to scope this API request.
                    type: string
                  tenantIdRef:
                    description: Reference to a Tenant in fusionauth to populate tenantId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  tenantIdSelector:
                    description: Selector for a Tenant in fusionauth to populate tenantId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timezone:
                    description: |-
                      The User’s preferred timezone. The string must be in an IANA time zone format.
                      The User’s preferred timezone. The string must be in an IANA time zone format. For example:
                    type: string
                  twoFactorMethods:
                    items:
                      properties:
                        authenticatorAlgorithm:
                          description: |-
                            The algorithm used by the TOTP authenticator. With the current implementation, this will always be HmacSHA1.
                          type: string
                        authenticatorCodeLength:
                          description: |-
                            The length of code generated by the TOTP. With the current implementation, this will always be 6.
                          format: int64
                          type: integer
                        authenticatorTimeStep:
                          description: |-
                            The time-step size in seconds. With the current implementation, this will always be 30.
                          format: int64
                          type: integer
                        email:
                          description: |-
                            The User’s email address. An email address is a unique in FusionAuth and stored in lower case.
                            The value of the email address for this method. Only present if user.twoFactor.methods[x].method is email.
                          type: string
                        method:
                          description: |-
                            The type of this method. There will also be an object with the same value containing additional information about this method.
                          type: string
                        mobilePhone:
                          description: |-
                            The User’s mobile phone number. This is useful is you will be sending push notifications or SMS messages to the User.
                            The value of the mobile phone for this method. Only present if user.twoFactor.methods[x].method is sms.
                          type: string
                        name:
                          description: |-
                            A user-friendly name for this two-factor method, for example "Work authenticator".
                          type: string
                        secretSecretRef:
                          description: |-
                            A base64 encoded secret
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  userId:
                    description: |-
                      The Id to use for the new User. If not specified a secure random UUID will be generated..
                      The Id to use for the new User. If not specified a secure random UUID will be generated.
                    type: string
                  username:
                    description: |-
                      The username of the User. The username is stored and returned as a case sensitive value, however a username is considered unique regardless of the case. bob is considered equal to BoB so either version of this username can be used whenever providing it as input to an API.
                    type: string
                  usernameStatus:
                    description: |-
                      The current status of the username. This is used if you are moderating usernames via CleanSpeak.
                    type: string
                  verificationIds:
                    description: |-
                      The list of all verifications that exist for a user. This includes the email and phone identities that a user may have. The values from emailVerificationId and emailVerificationOneTimeCode are legacy fields and will also be present in this list.
                    items:
                      properties:
                        verificationId:
                          description: |-
                            A verification Id.
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  applicationId:
                    description: |-
                      An optional Application Id. When this value is provided, it will be used to resolve an application specific email template if you have configured transactional emails such as setup password, email verification and others.
                    type: string
                  birthDate:
                    description: |-
                      An ISO-8601 formatted date of the User’s birthdate such as YYYY-MM-DD.
                    type: string
                  data:
                    description: |-
                      A JSON string that can hold any information about a User that should be persisted. Must be a JSON serialised string.
                      An object that can hold any information about a User that should be persisted. Must be a JSON encoded string.
                    type: string
                  disableDomainBlock:
                    description: |-
                      A tenant has the option to configure one or more email domains to be blocked in order to restrict email domains during user create or update. Setting this property equal to true will override the tenant configuration. See registration_configuration.blocked_domains in the Tenant resource.
                      A tenant has the option to configure one or more email domains to be blocked in order to restrict email domains during user create or update.
                    type: boolean
                  email:
                    description: |-
                      The User’s email address. An email address is a unique in FusionAuth and stored in lower case.
                    type: string
                  encryptionScheme:
                    description: |-
                      The method for encrypting the User’s password.
                    type: string
                  expiry:
                    description: |-
                      The expiration instant of the User’s account. An expired user is not permitted to login.
                    format: int64
                    type: integer
                  factor:
                    description: The factor used by the password encryption scheme.
                      If not provided, the PasswordEncryptor provides a default value.
                      Generally this will be used as an iteration count to generate
                      the hash. The actual use of this value is up to the PasswordEncryptor
                      implementation.
                    format: int64
                    type: integer
                  firstName:
                    description: |-
                      The first name of the User.
                    type: string
                  fullName:
                    description: |-
                      The User’s full name as a separate field that is not calculated from firstName and lastName.
                    type: string
                  imageUrl:
                    description: |-
                      The URL that points to an image file that is the User’s profile image.
                    type: string
                  lastName:
                    description: |-
                      The User’s last name.
                    type: string
                  middleName:
                    description: |-
                      The User’s middle name.
                    type: string
                  mobilePhone:
                    description: |-
                      The User’s mobile phone number. This is useful is you will be sending push notifications or SMS messages to the User.
                    type: string
                  parentEmail:
                    description: |-
                      The email address of the user’s parent or guardian. This field is used to allow a child user to identify their parent so FusionAuth can make a request to the parent to confirm the parent relationship.
                    type: string
                  passwordChangeRequired:
                    description: |-
                      Indicates that the User’s password needs to be changed during their next login attempt.
                    type: boolean
                  passwordSecretRef:
                    description: |-
                      The User’s plaintext password. This password will be hashed and the provided value will never be stored and cannot be retrieved.
                      The User's plaintext password. This password will be hashed and the provided value will never be stored and cannot be retrieved.
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  phoneNumber:
                    description: |-
                      The phone number of the User. The phone number is stored and returned in E.164 canonical format, however a phone number is considered unique regardless of the format. 303-555-1212 is considered equal to +13035551212 so either version of this phone number can be used whenever providing it as input to an API. If phone_number is not provided, then email or username will be required.
                    type: string
                  preferredLanguages:
                    description: |-
                      An array of locale strings that give, in order, the User’s preferred languages. These are important for email templates and other localizable text.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  sendSetPasswordEmail:
                    description: |-
                      Indicates to FusionAuth to send the User an email asking them to set their password. The Email Template that is used is configured in the System Configuration setting for Set Password Email Template. Use send_set_password_identity_type instead.
                      Indicates to FusionAuth to send the User an email asking them to set their password. The Email Template that is used is configured in the System Configuration setting for Set Password Email Template.
                    type: boolean
                  sendSetPasswordIdentityType:
                    description: |-
                      If set, FusionAuth will send the User a message asking them to set their password. When you set this value to anything but `doNotSend`, any provided password field is ignored. The possible values are `doNotSend`, `email`, and `phone`.
                    type: string
                  skipVerification:
                    description: |-
                      Indicates to FusionAuth that it should skip email verification even if it is enabled. This is useful for creating admin or internal User accounts.
                    type: boolean
                  tenantId:
                    description: |-
                      The unique Id of the tenant used to scope this API request.
                    type: string
                  tenantIdRef:
                    description: Reference to a Tenant in fusionauth to populate tenantId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  tenantIdSelector:
                    description: Selector for a Tenant in fusionauth to populate tenantId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  timezone:
                    description: |-
                      The User’s preferred timezone. The string must be in an IANA time zone format.
                      The User’s preferred timezone. The string must be in an IANA time zone format. For example:
                    type: string
                  twoFactorMethods:
                    items:
                      properties:
                        authenticatorAlgorithm:
                          description: |-
                            The algorithm used by the TOTP authenticator. With the current implementation, this will always be HmacSHA1.
                          type: string
                        authenticatorCodeLength:
                          description: |-
                            The length of code generated by the TOTP. With the current implementation, this will always be 6.
                          format: int64
                          type: integer
                        authenticatorTimeStep:
                          description: |-
                            The time-step size in seconds. With the current implementation, this will always be 30.
                          format: int64
                          type: integer
                        email:
                          description: |-
                            The User’s email address. An email address is a unique in FusionAuth and stored in lower case.
                            The value of the email address for this method. Only present if user.twoFactor.methods[x].method is email.
                          type: string
                        method:
                          description: |-
                            The type of this method. There will also be an object with the same value containing additional information about this method.
                          type: string
                        mobilePhone:
                          description: |-
                            The User’s mobile phone number. This is useful is you will be sending push notifications or SMS messages to the User.
                            The value of the mobile phone for this method. Only present if user.twoFactor.methods[x].method is sms.
                          type: string
                        name:
                          description: |-
                            A user-friendly name for this two-factor method, for example "Work authenticator".
                          type: string
                        secretSecretRef:
                          description: |-
                            A base64 encoded secret
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                      type: object
                    type: array
                  userId:
                    description: |-
                      The Id to use for the new User. If not specified a secure random UUID will be generated..
                      The Id to use for the new User. If not specified a secure random UUID will be generated.
                    type: string
                  username:
                    description: |-
                      The username of the User. The username is stored and returned as a case sensitive value, however a username is considered unique regardless of the case. bob is considered equal to BoB so either version of this username can be used whenever providing it as input to an API.
                    type: string
                  usernameStatus:
                    description: |-
                      The current status of the username. This is used if you are moderating usernames via CleanSpeak.
                    type: string
                  verificationIds:
                    description: |-
                      The list of all verifications that exist for a user. This includes the email and phone identities that a user may have. The values from emailVerificationId and emailVerificationOneTimeCode are legacy fields and will also be present in this list.
                    items:
                      properties:
                        verificationId:
                          description: |-
                            A verification Id.
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: UserStatus defines the observed state of User.
            properties:
              atProvider:
                properties:
                  applicationId:
                    description: |-
                      An optional Application Id. When this value is provided, it will be used to resolve an application specific email template if you have configured transactional emails such as setup password, email verification and others.
                    type: string
                  birthDate:
                    description: |-
                      An ISO-8601 formatted date of the User’s birthdate such as YYYY-MM-DD.
                    type: string
                  data:
                    description: |-
                      A JSON string that can hold any information about a User that should be persisted. Must be a JSON serialised string.
                      An object that can hold any information about a User that should be persisted. Must be a JSON encoded string.
                    type: string
                  disableDomainBlock:
                    description: |-
                      A tenant has the option to configure one or more email domains to be blocked in order to restrict email domains during user create or update. Setting this property equal to true will override the tenant configuration. See registration_configuration.blocked_domains in the Tenant resource.
                      A tenant has the option to configure one or more email domains to be blocked in order to restrict email domains during user create or update.
                    type: boolean
                  email:
                    description: |-
                      The User’s email address. An email address is a unique in FusionAuth and stored in lower case.
                    type: string
                  encryptionScheme:
                    description: |-
                      The method for encrypting the User’s password.
                    type: string
                  expiry:
                    description: |-
                      The expiration instant of the User’s account. An expired user is not permitted to login.
                    format: int64
                    type: integer
                  factor:
                    description: The factor used by the password encryption scheme.
                      If not provided, the PasswordEncryptor provides a default value.
                      Generally this will be used as an iteration count to generate
                      the hash. The actual use of this value is up to the PasswordEncryptor
                      implementation.
                    format: int64
                    type: integer
                  firstName:
                    description: |-
                      The first name of the User.
                    type: string
                  fullName:
                    description: |-
                      The User’s full name as a separate field that is not calculated from firstName and lastName.
                    type: string
                  id:
                    type: string
                  identities:
                    description: |-
                      The list of identities that exist for a User.
                    items:
                      properties:
                        displayValue:
                          description: |-
                            The display value for the identity. Only used for username type identities. If the unique username feature is not enabled, this value will be the same as user.identities[x].value. Otherwise, it will be the username the User has chosen. For primary username identities, this will be the same value as user.username.
                            The display value for the identity. Only used for username type identities. If the unique username feature is not enabled, this value will be the same as user.identities[x].value. Otherwise, it will be the username the User has chosen. For primary username identities, this will be the same value as user.username .
                          type: string
                        insertInstant:
                          description: |-
                            The instant when the identity was created.
                          format: int64
                          type: integer
                        lastLoginInstant:
                          description: |-
                            The instant when the identity was last used to log in. If a User has multiple identity types (username, email, and phoneNumber), then this value will represent the specific identity they last used to log in. This contrasts with user.lastLoginInstant, which represents the last time any of the User’s identities was used to log in.
                          format: int64
                          type: integer
                        lastUpdateInstant:
                          description: |-
                            The instant when the identity was last updated.
                          format: int64
                          type: integer
                        moderationStatus:
                          description: |-
                            The current status of the username. This is used if you are moderating usernames via CleanSpeak.
                          type: string
                        type:
                          description: |-
                            The identity type that the verification Id is for. This identity type, along with verificationIds[x].value , matches exactly one identity via user.identities[x].type.
                            The identity type.
                          type: string
                        value:
                          description: |-
                            The identity value that the verification Id is for. This identity value, along with verificationIds[x].type , matches exactly one identity via user.identities[x].value.
                            The value represented by the identity.
                          type: string
                        verified:
                          description: |-
                            Whether verification was actually performed on the identity by FusionAuth.
                          type: boolean
                        verifiedInstant:
                          description: |-
                            The instant when verification was performed on the identity.
                          format: int64
                          type: integer
                        verifiedReason:
                          description: |-
                            The reason the User’s identity was verified or not verified.
                          type: string
                      type: object
                    type: array
                  imageUrl:
                    description: |-
                      The URL that points to an image file that is the User’s profile image.
                    type: string
                  lastName:
                    description: |-
                      The User’s last name.
                    type: string
                  middleName:
                    description: |-
                      The User’s middle name.
                    type: string
                  mobilePhone:
                    description: |-
                      The User’s mobile phone number. This is useful is you will be sending push notifications or SMS messages to the User.
                    type: string
                  parentEmail:
                    description: |-
                      The email address of the user’s parent or guardian. This field is used to allow a child user to identify their parent so FusionAuth can make a request to the parent to confirm the parent relationship.
                    type: string
                  passwordChangeRequired:
                    description: |-
                      Indicates that the User’s password needs to be changed during their next login attempt.
                    type: boolean
                  phoneNumber:
                    description: |-
                      The phone number of the User. The phone number is stored and returned in E.164 canonical format, however a phone number is considered unique regardless of the format. 303-555-1212 is considered equal to +13035551212 so either version of this phone number can be used whenever providing it as input to an API. If phone_number is not provided, then email or username will be required.
                    type: string
                  preferredLanguages:
                    description: |-
                      An array of locale strings that give, in order, the User’s preferred languages. These are important for email templates and other localizable text.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  sendSetPasswordEmail:
                    description: |-
                      Indicates to FusionAuth to send the User an email asking them to set their password. The Email Template that is used is configured in the System Configuration setting for Set Password Email Template. Use send_set_password_identity_type instead.
                      Indicates to FusionAuth to send the User an email asking them to set their password. The Email Template that is used is configured in the System Configuration setting for Set Password Email Template.
                    type: boolean
                  sendSetPasswordIdentityType:
                    description: |-
                      If set, FusionAuth will send the User a message asking them to set their password. When you set this value to anything but `doNotSend`, any provided password field is ignored. The possible values are `doNotSend`, `email`, and `phone`.
                    type: string
                  skipVerification:
                    description: |-
                      Indicates to FusionAuth that it should skip email verification even if it is enabled. This is useful for creating admin or internal User accounts.
                    type: boolean
                  tenantId:
                    description: |-
                      The unique Id of the tenant used to scope this API request.
                    type: string
                  timezone:
                    description: |-
                      The User’s preferred timezone. The string must be in an IANA time zone format.
                      The User’s preferred timezone. The string must be in an IANA time zone format. For example:
                    type: string
                  twoFactorMethods:
                    items:
                      properties:
                        authenticatorAlgorithm:
                          description: |-
                            The algorithm used by the TOTP authenticator. With the current implementation, this will always be HmacSHA1.
                          type: string
                        authenticatorCodeLength:
                          description: |-
                            The length of code generated by the TOTP. With the current implementation, this will always be 6.
                          format: int64
                          type: integer
                        authenticatorTimeStep:
                          description: |-
                            The time-step size in seconds. With the current implementation, this will always be 30.
                          format: int64
                          type: integer
                        email:
                          description: |-
                            The User’s email address. An email address is a unique in FusionAuth and stored in lower case.
                            The value of the email address for this method. Only present if user.twoFactor.methods[x].method is email.
                          type: string
                        method:
                          description: |-
                            The type of this method. There will also be an object with the same value containing additional information about this method.
                          type: string
                        mobilePhone:
                          description: |-
                            The User’s mobile phone number. This is useful is you will be sending push notifications or SMS messages to the User.
                            The value of the mobile phone for this method. Only present if user.twoFactor.methods[x].method is sms.
                          type: string
                        name:
                          description: |-
                            A user-friendly name for this two-factor method, for example "Work authenticator".
                          type: string
                        twoFactorMethodId:
                          description: The unique Id of the method.
                          type: string
                      type: object
                    type: array
                  userId:
                    description: |-
                      The Id to use for the new User. If not specified a secure random UUID will be generated..
                      The Id to use for the new User. If not specified a secure random UUID will be generated.
                    type: string
                  username:
                    description: |-
                      The username of the User. The username is stored and returned as a case sensitive value, however a username is considered unique regardless of the case. bob is considered equal to BoB so either version of this username can be used whenever providing it as input to an API.
                    type: string
                  usernameStatus:
                    description: |-
                      The current status of the username. This is used if you are moderating usernames via CleanSpeak.
                    type: string
                  verificationIds:
                    description: |-
                      The list of all verifications that exist for a user. This includes the email and phone identities that a user may have. The values from emailVerificationId and emailVerificationOneTimeCode are legacy fields and will also be present in this list.
                    items:
                      properties:
                        oneTimeCode:
                          description: |-
                            A one time code that will be paired with the verificationIds[x].id.
                            A one time code that will be paired with the verificationIds[x].id .
                          type: string
                        type:
                          description: |-
                            The identity type that the verification Id is for. This identity type, along with verificationIds[x].value , matches exactly one identity via user.identities[x].type.
                            The identity type that the verification Id is for. This identity type, along with verificationIds[x].value , matches exactly one identity via user.identities[x].type .
                          type: string
                        value:
                          description: |-
                            The identity value that the verification Id is for. This identity value, along with verificationIds[x].type , matches exactly one identity via user.identities[x].value.
                            The identity value that the verification Id is for. This identity value, along with verificationIds[x].type , matches exactly one identity via user.identities[x].value .
                          type: string
                        verificationId:
                          description: |-
                            A verification Id.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
